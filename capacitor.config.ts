import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.asusoftware.cryptoverse',
  appName: 'Cryptoverse',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
